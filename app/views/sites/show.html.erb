<div>
  <h1><%= @site.name %></h1>
</div>
<div>
  <strong>Site Coordinates</strong>
</div>
<div>
  Latitude :<%= @site.latitude %>
</div>
<div>
  Longitude: <%= @site.longitude %>
</div>

<div>
  <strong>Weather Observation Station</strong>
</div>
<div>
  <%= @site.station_id %>
</div>

<% if @site.latest_precip %>
  <div>
    <strong>Precipitation</strong>
  </div>
  <div>
    Over Last 24 Hours: <%=@site.latest_precip %> inches.
  </div>
  <div>
    Over Last Week: <%=@site.week_precip %> inches.
  </div>
  <div>
    Precipitation Data Updated At: <%= @site.updated_at %>
  </div>
<% else %>
  <div>
    This address has not yet received precipitation data
  </div>
<% end %>

<%= button_to "Update Precipitation Data", {:controller => "sites", :action =>"update_rain", :id=> @site.id} %>

<div>
  <%= link_to "Back to your page", user_path %>
</div>

<div>
<%= link_to "Delete site", site_path(@site), method: :delete %>
</div>